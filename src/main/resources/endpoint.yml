endpoint:
  auth:
    signIn: /auth/signIn
  user:
    userInfo: /user/userInfo
    register: /user/register
    update: /user/update
    find: /user/find/{userId}
    findAll: /users
  role:
    register: /role/register
    update: /role/update
    find: /role/find/{roleId}
    findAll: /roles
  permission:
    register: /permission/register
    update: /permission/update
    find: /permission/find/{permissionId}
    findAll: /permissions